#!/bin/sh

set -e

if [ "$1" = "configure" ] \
   && [ -e /var/singularity ]; then
   for sd in '' /mnt /mnt/source; do
   	   d=/var/singularity$sd;
	   if [ `stat -c '%a' $d` = 700 ]; then
	       # Force global access permissions needed since 2.2
		   chmod 0755 $d
	   fi
   done
fi
